import { SetMaxHeightDirective } from './set-max-height.directive';

describe('SetMaxHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SetMaxHeightDirective();
    expect(directive).toBeTruthy();
  });
});
